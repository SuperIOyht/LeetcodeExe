#include<iostream>
using namespace std;

  struct ListNode {
      int val;
      ListNode *next;
      ListNode(int x) : val(x), next(NULL) {}
 };

  class Solution {
  public:
	  ListNode* reverseKGroup(ListNode* head, int k) {
		  ListNode* reverseNode = head, *pre = head, *now = head->next, *nextNode;
		  int nodeNum = 0;
		  while (nodeNum <= )
		  {
			   
		  }
	  }
  };
 