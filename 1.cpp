#include"headfile.h"
#include<vector>
using namespace std;

int main() {
	ListNode* a = new ListNode();
	priority_queue<int> pq;
	unordered_map<int, int> hash;
	vector<int> arr = vector<int>(10, 0);
	vector<int> dualstack;
	dualstack =  vector<int>(5);
	stack<int> mystack;
	queue<int> myqueue;
	hash.count(1);
}

ListNode* division(ListNode* head) {

}